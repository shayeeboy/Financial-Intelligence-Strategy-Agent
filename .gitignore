node_modules/
.env
.env.local
*.log
.DS_Store
# Cloudflare Worker local state / secrets — never commit
.wrangler/
.dev.vars
# Ignore raw snapshots by default (regenerable), but keep the one that backs
# the committed example brief so its provenance is inspectable in the repo.
data/raw/*.json
!data/raw/snapshot_gta_newcomer_credit_opportunity.json

# Sensitive meta-prompt — kept local, never cloned/forked (see .git_prompts/README.md)
.git_prompts/MASTER_REVERSE_ENGINEER_PROMPT.md
